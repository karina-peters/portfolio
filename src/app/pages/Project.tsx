// import type { Route } from "./+types/team";

// export async function loader({ params }: Route.LoaderArgs) {
//   return params.id;
// }

export function Project(/*{ params }*/) {
  return <main>{/* TODO: render markdown files */}</main>;
}
